#!/bin/bash

cwd="$(pwd)"
cd /workspace/resumator/resumator
cmd="/usr/bin/env php app/cli app:init --behavior local --stack newlocal --cache-clear resumatordev.com -vvv"
echo "Running: $cmd"
$cmd

ret="$?"

[ -d "$cwd" ] && cd $cwd

exit $ret